const initialState = {
  friendList: [],
  selectedUser: {},
  selectedUserResults: []
};

export default function friendReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
