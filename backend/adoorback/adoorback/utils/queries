
첫 글자가 대문자면 모델 이름, 첫 글자가 소문자면 object인 것으로 생각해주세요! (e.g. `Response` = 답변 모델, `response` = 답변 모델의 인스턴스)

1. 질문의 답변을 찾고 싶어요
- `response.target`

2. 모든 질문들을 찾고 싶어요
- `Question.objects.all()`

3. Custom Question만 찾고 싶어요
- `CustomQuestionPost.objects.all()`

.
.
.

(그냥 모를 때마다 그떄그떄 물어봐주시면 제가 정리를 해두겠습니다 미리 하자니 너무 많음)
(웬만한건 `shell` 파일에 있는 query만 봐도 알 수 있을거에요~!)